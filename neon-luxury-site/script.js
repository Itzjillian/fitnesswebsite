document.getElementById('year').textContent = new Date().getFullYear();
const menuBtn=document.getElementById('menuBtn');const nav=document.getElementById('nav');
menuBtn?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',open?'true':'false');});
const chatBtn=document.getElementById('chatBtn');const chatBox=document.getElementById('chatBox');const chatInput=document.getElementById('chatInput');const chatSend=document.getElementById('chatSend');
chatBtn?.addEventListener('click',()=>{const hidden=chatBox.hasAttribute('hidden');if(hidden)chatBox.removeAttribute('hidden');else chatBox.setAttribute('hidden','');});
chatSend?.addEventListener('click',()=>{const val=(chatInput.value||'').trim();if(!val)return;const body=chatBox.querySelector('.chatbox__body');const me=document.createElement('p');me.innerHTML='<strong>You:</strong> '+val;body.appendChild(me);const reply=document.createElement('p');let text='Thanks! I will reply by email soon.';if(/price|cost|how much/i.test(val))text='Coaching starts at $X/mo. Digital programs from $Y.';if(/equipment|gym/i.test(val))text='Any commercial gym works. Machines are fine; we bias hip hinges and single-leg work.';if(/program|plan/i.test(val))text='Three tracks: Glute Atelier, Athlete Strength, Mobility Ritual. Apply on the site.';reply.innerHTML='<strong>Bot:</strong> '+text;body.appendChild(reply);body.scrollTop=body.scrollHeight;chatInput.value='';});